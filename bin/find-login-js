#!/bin/bash
echo login/tootsville.js
find login -name '*.js' \
     -and -not -name login.js \
     -and -not -name tootsville.js \
     -and -not -name start.js \
     -and -not -name '.#*' \
     -and -not -path lib/** \
    | sort
find include -name \*.js
echo login/start.js
